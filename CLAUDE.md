# CLAUDE.md

## What this is
A native-Rust BMW diagnostic and coding tool for F-series BMWs, talking over Ethernet (ENET cable) directly to the car. Two real cars exist: the original target **F20 1-series** (gateway `169.254.90.33`, the 2026-07-03 captures) and an **F25 X3** (gateway `169.254.71.121`, CAS4 not FEM, car session 1 + the iOS probe) — same N47 DDE `d72n47a0`, so offline DDE work applies to both. Check a capture's ident VIN before assuming which car it is. Two faces: a library/CLI now, and later an MCP server so an agent can read faults and live data and reason about them. The long-term value is the semantic layer — turning raw protocol exchanges into "here's what's wrong and why" — not the protocol itself.

The protocol spec lives in `docs/protocol-reference.md`. Treat it as the source of truth for frame layouts, the UDS service catalog, the HSFZ handshake, ports, and gateway addressing. Don't re-derive protocol details from memory; read the report.

## Scope (decided — don't expand without being asked)
In:
- Diagnostics: read DTCs, read live data/identifiers, clear DTCs, run service functions (routine control). This is the recurring use and the priority.
- Replay-coding: read a module's NCD, patch known byte changes, write it back. No coding-definition decoding.
Out (do not build unless explicitly asked):
- A general FDL/CAFD editor (editing any coding parameter by name). Deferred; needs a full PSdZData CAFD parser.
- ECU flashing/programming (UDS transfer services 0x34–0x37). Out entirely beyond awareness.

## Architecture
Bottom to top:
1. Transport — HSFZ (BMW-proprietary, F-series) over TCP. Implement concretely now.
2. UDS (ISO 14229) — request/response service layer on top of transport.
3. Semantic — meaning of the bytes (DID scaling, DTC meaning, service-function recipes), built from ISTA's data and captures. A later milestone.

DoIP (ISO 13400) is the G-series transport and a FUTURE addition. Do NOT build a transport trait/abstraction for it yet — there is one transport today. Implement HSFZ as a concrete type behind a clean module boundary; extract a trait when DoIP is actually added. No speculative abstraction.

## Hard rules
- BYO-data. Never commit BMW's proprietary data: ISTA SQLiteDBs, PSdZData, or packet captures (they contain the VIN). Gitignore `captures/`, `*.pcap`, `*.pcapng`, and any data dirs. The repo ships empty of BMW data; the user supplies their own.
- Safety by blast radius (encode as layers are built): reads (DTCs, live data, identifiers) are safe and may run autonomously. Writes — NCD coding writes and actuation (IO control, state-changing routine control) — must require explicit confirmation and must read+back-up the original bytes before writing. Flashing: unsupported.
- License: AGPL-3.0. Implement protocols from the report and ISO standards (frame layouts and handshakes are facts, not copyrightable). Do NOT copy code from reference libraries — especially Scapy (GPLv2), which would force its license. Read them to understand; reimplement in your own code.

## Stack
- Latest stable Rust, edition 2024. Async via tokio.
- Before hand-writing standardized layers, check crates.io: there may be usable UDS crates, and a Rust MCP SDK (check the current crate, e.g. rmcp) for the later MCP milestone. HSFZ is proprietary and niche — write it yourself regardless.
- SQLite parsing for the semantic layer (later) via rusqlite or sqlx — defer until that milestone.
- Cargo workspace (chosen up front for a reusable core that future binaries share). Layout convention: **library crates live under `crates/`; each binary lives in its own top-level directory** — no `bin/` grouping dir. Today: libraries `crates/uds` (pure UDS messages), `crates/hsfz` (concrete HSFZ transport), `crates/client` (managed UDS session + typed read/clear services over HSFZ), `crates/semantic` (ISTA-DB-backed DTC/DID decoding; exposes the general ECU map via `Catalog::ecus()`); binaries `cli/` (`klartext-cli`, builds the `klartext` binary) and `mcp/` (`klartext-mcp`, the read-only stdio MCP server over `klartext-client` + `klartext-semantic`). Dirs are short; package names keep the `klartext-` prefix. Shared versions/metadata via `[workspace.dependencies]` and `[workspace.package]`. Still do NOT pre-create empty crates for layers that don't exist yet — when a milestone needs it, add a new **library** under `crates/` (`klartext-doip`, or a `klartext` facade) and a new **binary** as its own top-level dir.

## Conventions
- Errors: thiserror for library types, anyhow at the binary boundary.
- cargo fmt and cargo clippy -- -D warnings clean before a milestone is done.
- Conventional commits.

<anti-overengineering>
Do NOT:
  - Add traits/interfaces for things with one implementation today (no Transport trait until DoIP exists).
  - Create config systems or plugin layers for values with one setting.
  - Pre-create empty crates for the MCP or semantic layer before their milestone (the workspace exists; its future sibling crates do not — add each when its milestone needs it).
  - Generate "future-proof" abstractions beyond what the current milestone asks.
  - Leave throwaway test/scratch files uncleaned.
Do:
  - YAGNI ruthlessly. One concrete implementation, inlined where single-caller.
  - Hardcode values with one legitimate setting; mark protocol values the report flagged "verify against capture" as clearly-named constants.
  - Write the minimum that passes the milestone's verify checklist.
</anti-overengineering>

## Hardware-in-the-loop
You (Claude) cannot reach the car. Unit-test frame encode/decode against known byte vectors from the report (and from a capture if one is in the repo). The end-to-end test against the real gateway is a MANUAL step the human runs. Keep the two separate — never claim a hardware round-trip works; only that unit tests pass and the manual test is ready.


## MCP server (M4)
- New crate klartext-mcp: a stdio MCP server exposing klartext's diagnostic READS as tools, so an AI client (Claude Desktop / Claude Code) can read and reason about the car. Reuses klartext-client and klartext-semantic; adds no new car/protocol logic.
- **Blast radius is a TIER LADDER, not a read-only wall (REWRITTEN 2026-07-18 for P3 — see `docs/superpowers/specs/2026-07-18-p3-service-write-tier-design.md`).** The prior invariant ("NO physical actuation is ever MCP-executable; those writes stay in the CLI") is **deliberately REVERSED** by owner decision: he services his own cars, ISTA actuates on a button press without further prompting, and the CLI cannot show live data while a component moves — so a blanket agent-surface ban blocked the actual use case. The ladder:
  - **Read** (faults, freeze-frames, measurements, identity, info memory, `run_job`) — MCP + CLI, no gate, autonomous-safe.
  - **Standard clear** (`14 FF FF FF`, plus the new post-clear `0x11` ECU reset) — MCP + CLI, needs `confirm=true` relayed from the human; pre-read+record each ECU's DTCs before erasing, post-read to verify. `clear_all_faults` is the SAME frame batched — not a new capability.
  - **Service write** (resets, adaptations, **actuation**, calibration) — MCP + CLI, needs `confirm=true` **plus** preconditions **plus** the `Reset` phase on failure. Executed by running ISTA's own EDIABAS job through the BEST/2 VM with `job_param` arguments, behind `Policy::ConfirmedWrite` at the transmit seam.
  - **Flashing** (`0x34`–`0x37`) — never implemented, unchanged.
  Preconditions (not categorical bans) are the safety mechanism; an unresolvable check degrades to advisory rather than refusing. The M6 dynamic-READ define (`0x2C`, session-transient read plumbing) remains the deliberate read-side exception. `Policy::ReadOnly` stays the default for every read path — `run_job`'s guarantee is untouched.
- M10 made discovery/naming fully live: `scan_ecus` probes for the FITTED ECUs (not the 170-entry model map); ECU names come from the ISTA DB (no hardcoded aliases — the old "DME"/"CAS" mislabelled this diesel/FEM car); a `variant` resolves via a ladder (explicit → learned per-VIN profile → single DB candidate) so it need not be hand-supplied; one demultiplexed HSFZ connection reaches every ECU; and the server disconnects on exit.
- Uses the official rmcp crate (CURRENT version — verify on crates.io/docs.rs; its macro API has changed across versions, so follow current rmcp examples, not older tutorials).
- stdio transport. CRITICAL: nothing may write to stdout except the JSON-RPC stream — any stdout logging corrupts the transport and the client silently disconnects. Route ALL logging to stderr.
- Same BYO-data boundary: ISTA SQLiteDB path via env/arg, read-only; never embed or commit DB contents.
- Milestone order: M4 MCP server (reads), then later gated service-function recipes / replay-coding (writes) and the SGBD-based DID scaler.

## Standard-PID scaling (M5)
- Extend klartext-semantic with engineering-unit scaling for STANDARD OBD-II / SAE J1979 PIDs only — public, documented formulas (e.g. RPM = ((A*256)+B)/4). No SGBD/proprietary data.
- read_data and read_did return name + scaled value + unit for a recognized standard PID; unrecognized DIDs fall back to the existing named/raw behavior (never error on unknown — degrade to raw).
- Proprietary BMW DIDs (SGBD-defined scaling) are explicitly OUT — that's a later milestone after the SGBD format is cracked. Do not guess proprietary formulas.
- Formulas are pure functions, fully unit-testable offline against known input→output vectors. Real-car confirmation is a later manual step, but the math is verifiable now.
- LIVE FINDING (2026-07-03, verified against the F20 capture): this DDE does NOT map the SAE `0xF4xx` PIDs onto UDS 0x22 — `read_data F40C`/`F405` return `7F 22 31` (requestOutOfRange). The M5 scaler is correct but a no-op on this car; live data must come from the SGBD `SG_FUNKTIONEN` measurements (the M6 path). The capture also confirmed the M6 `2C`/`22` dynamic-read sequence byte-for-byte.

## Next milestones (M11+) — owner-requested, not yet started
See `docs/superpowers/specs/2026-07-03-m11-ista-parity-roadmap.md` for the full brief. In owner-priority order: (1) **freeze-frame metadata on faults** (km/RPM/state at occurrence — UDS `19 04`/`19 06`; today's read is code+status only. NOTE: the fault-memory *filter* already matches what the owner wants — showing active faults is fine — so do NOT change `RELEVANT_MASK`; only add snapshot reads); (2) **SVT + full identification dump** (ISTA-style "which ECUs exist by name + variant" in one gateway read — decide the read-vs-CLI invariant for its `0x31` frames); (3) **`embed-data` cargo feature** (self-contained binary via `include_bytes!`/`SQLite::deserialize` — PERSONAL-USE only, never published, per BYO-data); (4) **ISTA repair-doc catalog** (fault→procedure links from `DiagDocDb`); (5) **BEST-2/ABL guided multi-step procedures** (the big one; shares infra with the inline-scaling tail); (6) **mobile (iOS) over USB-C Ethernet** (Rust core via UniFFI + SwiftUI). **Transport de-risked on real hardware 2026-07-10** (`docs/field-findings-2026-07-10-ios.md`): unicast HSFZ ident answers in 2 ms → discovery needs NO multicast entitlement (sweep or passive listen — the ZGW self-announces on 6811 at wake); plain BSD `connect()` to 6801 works in 1 ms while `NWConnection` fails on link-local (`ENETDOWN`) → keep the socket path in Rust std/tokio, never Network.framework; don't bind to the interface address (breaks egress — use `IP_BOUND_IF` if pinning is ever needed). Highest-leverage research step for 1–4: decompile the ISTA .NET DLLs (`ilspycmd`) to read exactly how it reads/clears faults + runs procedures, then reimplement (facts only, never copy — AGPL). **DONE 2026-07-10 — the ISTA stack is on disk (`data/TesterGUI/bin/Release/`, 147 DLLs) and the source catalog DBs are decryptable with the existing build script.** Full gap analysis: `docs/superpowers/specs/…` + the memory `ista-catalog-gaps-and-index`. **Headline finding = the highest-leverage data-extraction milestone: ISTA's MEASUREMENT/JOB CATALOG — ✅ PHASE 1 DONE + committed 2026-07-12 (`8aaeef0`).** Extracted `XEP_ECURESULTS` (result name + unit + linear scaling + owning job) to the semantic DB `measurement` table via `scripts/build-semantic-db.sh` (RC4, `key='6505EFBDC3E5F324'`), + `Catalog::measurements(variant)`/`MeasurementCatalogEntry` API + tests. The 6-hop function-tree join is CONFIRMED against the real DiagDocDb (`XEP_ECUVARFUNCTIONS(NAME=variant)`→`REFECUFUNCSTRUCTS`→`ECUFUNCSTRUCTURES`→`ECUFIXEDFUNCTIONS(PARENTID)`→`REFECURESULTS`→`ECURESULTS`, job via `ECURESULTS.ECUJOBID`); variant-scoping essential (d72n47a0=86 vs group 521); ~50k rows/1,281 variants. This is the "index" the owner sensed — ISTA-grade measurement labels + scaling as DATA (the BEST/2 VM still decodes VALUES — complementary). **Owner must REBUILD the semantic DB to populate it (one command, `scripts/build-semantic-db.sh` — now also emits `job_param`, `bordnet_doc`, and via docbuild `ecu_tree`/`ecu_housing`).** ✅ 2026-07-17 (commits `3fa4f5f`…`e08c509`): `list_measurements` catalog fallback (MCP) + CLI `measurements` subcommand + SGBD entries enriched with the catalog job name; `XEP_ECUPARAMETERS` extracted (`job_param`, 63k rows — per ISTA fixed function the job + positional args whose `;`-join is the EDIABAS arg buffer; CLI `job args <JOB>` + `Catalog::job_parameters`); **ECU topology solved** — NOT via `XEP_ECUGROUPS` flags (verified dead end: OBDIDENTIFICATION = emissions groups) but via the per-platform **BNT-XML bordnets** (`XEP_INFOOBJECTS 'BNT-XML-%'` → doc id → XML in plaintext `xmlvalueprimitive_OTHER.sqlite`): 71 platforms/4,019 rows → `ecu_tree` (address → ISTA short name, bus+label, grid pos, **minimal-config flag — exactly 11 on F25_1404, reproducing ISTA's graph view**) + `ecu_housing`; served by `Catalog::ecu_tree`, offline CLI `ecu-tree [series]`, and live enrichment on CLI `scan` + MCP `scan_ecus`/`identify_vehicle` (client `IStufeLevels` reads all 3 I-Stufe records; `bordnet_series_for` maps the FACTORY level via the dated-variant ladder, F025-17-03→F25_1404). Remaining catalog gaps, in order: the diagnosis/symptom/measure catalog (`XEP_DIAGNOSISOBJECTS` 96k / `XEP_PERCEIVEDSYMPTOMS` / `XEP_SWIACTION` — the Item-5 guided-procedure spine), the characteristics/coding catalog (`XEP_CHARACTERISTICS`), and supporting id indexes (`XEP_ISTUFEN`/`XEP_SALAPAS`/`XEP_FAULTCLASSES`/`VINRANGES`) — all `SELECT` denormalizations. Separate harder LOGIC ports (not data): the `XEP_RULES` offline-fitment engine and the ABL/service-program interpreter (the EcuTree reduction is no longer needed — the bordnet data covers it).

**M11 Item 2 done:** the SVT read is UDS `22 3F 07` (a read), exposed on the CLI (`identify`, `scan`) and MCP (`identify_vehicle`); the `0x31 STEUERN_VCM_GENERATE_SVT` stays out. **UPDATE 2026-07-10 (owner 32-vs-11 catch):** `22 3F07` = `STATUS_VCM_GET_ECU_LIST_ALL` = the VCM **CONFIGURED superset** (not "fitted"; ISTA reads the same 32 and post-filters to ~11 via per-model bus/housing rules). Relabeled configured across CLI/MCP (`FittedEcuInfo`→`ConfiguredEcuInfo`) + added the **actively-responding subset** `22 3F08` (`STATUS_VCM_GET_ECU_LIST_ACTIVE_RESPONSE`, `read_responding_ecu_list`) as a best-effort per-ECU `responding` flag + count (`[verify against capture]`). ISTA-parity ~11 needs the ECU-topology table (`XEP_ECUGROUPS` VIRTUELL/OBDIDENTIFICATION + bus/housing reduction), a future extraction.

**M11 Item 4 done (link+title layer):** the ISTA repair-doc catalog — fault→doc bridge `RG_ECUFAULT_DOCIDS.ECUFAULT_ID = XEP_FAULTCODES.ID` joined to `XEP_INFOOBJECTS` titles/type/docnumber/safety, extracted into the semantic DB — surfaced OFFLINE (no car) via CLI `fault-docs <code>` and MCP `fault_help`; the 50 GB document PROSE stays deferred (per-language content IDs preserved in `fault_doc`). **Item 3 (`embed-data`) deferred to the mobile milestone (item 6):** desktop payoff is marginal and any bundled `.prg` subset is arbitrary — revisit when the iOS core actually needs a self-contained payload.

**BEST/2 VM — Item 5 P1 (engine completion) DONE + merged (2026-07-07, merge `26a0d5f`).** Crate `klartext-best` is now a complete offline BEST/2 engine: full-range decode (the Phase-1 "raw-only" finding was a **first-`eoj` truncation artifact** — the bytecode DOES walk `SG_FUNKTIONEN`/`RES_` tables and scale; the generic `STATUS_LESEN`/`STEUERN_IO`/`STEUERN_ROUTINE` framework jobs are byte-identical across F20 ECUs), `Operand::Indexed` read/write, trap state (`jt`/`jnt`/`settmr` = trap-mask ops), `wait` as async Flow, overlapping B/I/L register file (`[u8;32]` LE — Phase 1's separate arrays were a genuine bug), and `run_job(name, target, args, exchange)`. **Proven on real data:** real DDE jobs run to `eoj`; the differential oracle is green (VM `STATUS_LESEN` == `measurement.rs` ±1e-6 — `measurement.rs` STAYS as the DDE fast path + oracle); a DSC read decodes 40 results/15 named stems (the owner's multi-value requirement, discriminatingly tested). Spec: `docs/superpowers/specs/2026-07-06-item5-guided-service-procedures-design.md`. **Next = P2 (spec §9): live read path** — `FnExchange` over `Session::request`, `GatedExchange(ReadOnly)`, CLI `job run/list`, MCP `run_job`, multi-value surfacing — then the owner's car session 1 (capture gate, `docs/on-car-verification-protocol.md` + pcap), then P3 (guided oil-level flow + the one gated write + CLAUDE.md invariant rewording per the approved spec §6). P2 facts already frozen in `crates/best/tests/differential.rs` docs: jobs transmit BMW-FAST telegrams `[0x80|len][tgt][src][uds][cksum]`; `STATUS_LESEN` args are `"<SPALTE>;<value>"` (e.g. `"ARG;ITOEL"`); `_INFO` text needs Latin-1 decode (cosmetic follow-up).

**BEST/2 VM — Item 5 P2 (live read path) DONE + merged (2026-07-07, merge `203bbf3`).** CLI `job list`/`job run <JOB> <args…>` and MCP `run_job` (read-only) execute real EDIABAS read jobs against the car. Live stack (composed outermost→in in each binary; `klartext-best` never depends on `klartext-client` — they meet only in the binaries' `SessionBridge`): `GatedExchange::read_only(TelegramExchange::new(SessionBridge{client}))`. The **read-only gate** classifies the outgoing telegram's UDS SID (form-aware `peek_sid`, sharing `header_and_length` with `decode_request` so the classified byte == the transmitted byte — no long-form-write bypass) and refuses any write/actuation/flashing SID **at the transmit seam** on BOTH surfaces (`Policy::ReadOnly`; the invariant is verified end-to-end + behaviorally on the wire). The **BMW-FAST codec** (`telegram.rs`) is form-aware (short / 8-bit / 16-bit long) with the additive `CalcChecksumBmwFast`; the VM's `xsend` request is checksum-LESS (the interface appends it) so it decodes checksum-lenient, while responses re-encode with the additive checksum a real ECU sends. `_INFO` mojibake was a REAL bug — `write_string` UTF-8-encoded the String↔S-register boundary where EDIABAS uses CP1252 — fixed both sides via a now-`pub klartext_sgbd::cp1252`. **Then P3:** `Policy::ConfirmedWrite` + the guided oil-level flow + the one gated `LERNWERTE_RUECKSETZEN` write + the CLAUDE.md tier-ladder rewrite (spec §6).

**CAR SESSION 1 DONE (2026-07-10) — on a SECOND car (the F25 X3), chassis-portability proven.** Full report + byte-level pcap verification: `docs/car-session-1-results.md` (pcap: `captures/captures/car-session-1.pcapng`). **Wire-confirmed:** the whole M0–M10 read stack (HSFZ, link-local discovery, SVT `62 3F07` = u16-BE count + 1 byte/ECU, DTC `59 02` 4-byte records, freeze-frame `19 04`/`06` reads, M6 dynamic-define scaling — DPF soot measured 15.49 g vs modelled 15.5 g), the read-only gate ON the wire (41-min TX census: `22/19/2C/3E` + one `10 03`+`14 FF FF FF`→`54` = the confirmed clear; zero other write SIDs; `FS_LOESCHEN` refused pre-transmit), and BEST/2 markers #1/#2 (the DDE parsed our BMW-FAST telegram + additive checksum and answered a valid NRC). **But the P2 litmus FAILED — the one real bug (= P2.1, next code work): the VM transmits the SG_FUNKTIONEN table-id as the UDS DID (`22 4517` → `7F 22 31`) where `measurement.rs` resolves the same measurement via `2C 01 F303 4517 01 02` + `22 F303` → 46 °C, twenty seconds apart in the same pcap.** The offline oracle missed it because it validated response decoding only, never the emitted request. **ROOT CAUSE SETTLED (2026-07-10 — A2, via klartext's own `decode_job` on the DDE bytecode + the on-disk ISTA decompile + the on-wire capture): the VM is FAITHFUL — generic `STATUS_LESEN` is a STATIC-only reader (27,615 ops but exactly ONE `xsend`; reads `SG_FUNKTIONEN.SERVICE`, validates static `"22"`; ZERO `2C`/`F303` literals), so there is NO VM bug (A1 false). The `2C` define lives in 44 dedicated `STATUS_<X>` jobs (e.g. `STATUS_MOTORTEMPERATUR` = 3 xsends) and in `measurement.rs::build_read_request` (byte-identical, on-car proven); ITOEL has no dedicated job. FIX = job SELECTION, not VM execution: route dynamic (`SERVICE`⊇`2C`) measurements through the existing `build_read_request` path (klartext already does in `read_data`/`read_did` → `read_dynamic_measurement`), and make `run_job STATUS_LESEN` refuse/redirect a dynamic row instead of emitting a doomed static `22`. The VM stays correct+needed for STATIC reads (incl. structured RES_ multi-value, which `measurement.rs` CANNOT decode), dedicated dynamic jobs, and service functions — so it is NOT sidelined.** Also extend the oracle to assert on the emitted request. **FIX IMPLEMENTED + verified 2026-07-10 (both surfaces, workspace `fmt`/`test`/`clippy -D warnings` green): `klartext_semantic::misrouted_dynamic_measurement` + a guard in CLI `job run` and MCP `run_job` redirect a dynamic (`SERVICE`⊇`2C`) measurement to `read_data`/`read-data` (the on-car-proven selektiv-lesen path) instead of a doomed static `22`; `STATUS_LESEN` stays for static + structured reads.** On-car litmus is now `read_data ITOEL`→46 °C (already proven) + the DSC structured multi-result read; the old doomed `job run STATUS_LESEN ARG ITOEL` step is replaced by that redirect. Still pending on-car: DSC structured multi-result (never captured), CP1252 `_INFO`, and driving a 3-`xsend` dedicated job through the VM. **Also from session 1:** learned per-VIN variant profile is NOT a bug — VERIFIED works-as-designed 2026-07-10 (`read_data` records VIN→address→variant on a scaled read `server.rs:932`, keyed by the connect VIN; the M10 ladder `resolve_variant` auto-resolves later reads; the "pass variant" message is the expected FIRST-use state — seed a many-candidate ECU once, then it resolves; unit-tested `resolve_variant_uses_a_learned_profile_keyed_by_vin`); ECU `title` null everywhere was NOT a code bug — the current DB has titles for all 1 402 rows and the code (`Catalog::ecus`→`mcp/src/ecu.rs:78`/`server.rs:1718`) propagates them (verified via the passing unit test + replaying the exact runtime queries incl. the `pragma_table_info(?1)` check); the session used a DB build predating title population → rebuild resolves, no code change; **Infospeicher not read** — klartext reads only fault memory `19 02`; BMW's secondary/info memory is read via **`22 2000`** (jobs `IS_LESEN`/`IS_LESEN_DETAIL`, detail `22 20 nn`; confirmed 2026-07-10 from the DDE `.prg` bytecode — a DISTINCT memory from `19 02`, own tables + event flag, pure `0x22` read, 342/1405 ECUs document it) and is what ISTA shows alongside faults — **read-only `read_info_memory` (MCP) + `info-memory` (CLI) tool IMPLEMENTED + verified 2026-07-10** (uds `decode_info_memory`/`did::INFO_MEMORY`, client `read_info_memory`, entries reuse the `Dtc`/`fault_info` text decode; response record-width `[verify against capture]`, raw payload surfaced for the on-car capture); relevant to the owner's AUC-sensor question (results doc); **I-Stufe layout CRACKED** (3×8-byte records: ASCII series + BINARY year/month + u16-BE patch → current `F025-23-07-530` / previous `F025-20-07-540` / factory `F025-17-03-505`; binary-not-BCD confirmed by the owner's 2017 build) — decoder now IMPLEMENTED + tested (`crates/client` `decode_i_stufe`; `read_i_stufe` returns the current level, `[verify against capture]` marker flipped to confirmed, client+uds gates green); FA `62 3F06` 214-byte SALAPA test vector captured (decode open); ~10/32 ECUs time out under the rapid whole-car sweep though they answer individually (wake/retry strategy); gateway FIN+RST on ignition-off drops the session with no auto-reconnect. Vehicle itself: one genuine fault (DAB antenna open circuit, re-sets after clear), DPF healthy.
