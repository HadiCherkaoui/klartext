import SwiftUI

@main
struct KlartextProbeApp: App {
    var body: some Scene {
        WindowGroup {
            // Replaced by ProbeView() in Task 3 once the UI exists.
            Text("KlartextProbe")
                .font(.title2)
                .padding()
        }
    }
}
