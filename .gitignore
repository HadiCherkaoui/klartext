/target

# BYO-data: never commit BMW proprietary data or captures (they contain the VIN).
captures/
*.pcap
*.pcapng
data/

# Claude Code: skills are vendored and shared (.claude/skills/ — see its README);
# only machine-local settings stay untracked.
.claude/settings.local.json
