/target

# BYO-data: never commit BMW proprietary data or captures (they contain the VIN).
captures/
*.pcap
*.pcapng
data/

# Local Claude Code config + skills (installed per-machine as needed, not shared).
.claude/
